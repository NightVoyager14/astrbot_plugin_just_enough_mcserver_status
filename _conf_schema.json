{
  "ping_icon": {
    "description": "延迟图标",
    "type": "object",
    "hint": "延迟图标展示设置",
    "items": {
      "is_opened": {
        "description": "延迟图标显示",
        "hint": "是否开启延迟图标在信息卡片中渲染",
        "type": "bool",
        "default": true
      },
      "excellent": {
        "description": "Excellent",
        "type": "int",
        "hint": "延迟优秀",
        "default": 50
      },
      "good": {
        "description": "Good",
        "type": "int",
        "hint": "延迟较好",
        "default": 100
      },
      "medium": {
        "description": "Medium",
        "type": "int",
        "hint": "延迟一般",
        "default": 200
      },
      "bad": {
        "description": "Bad",
        "type": "int",
        "hint": "延迟较差",
        "default": 500
      }
    }
  },
  "server_icon": {
    "description": "服务器图标",
    "type": "object",
    "hint": "服务器图标展示设置",
    "items": {
      "is_opened": {
        "description": "服务器图标显示",
        "hint": "是否开启服务器图标在信息卡片中渲染",
        "type": "bool",
        "default": true
      }
    }
  },
  "server_title": {
    "description": "服务器标题",
    "type": "object",
    "hint": "服务器标题展示设置",
    "items": {
      "is_opened": {
        "description": "服务器标题显示",
        "hint": "是否开启服务器标题在信息卡片中渲染",
        "type": "bool",
        "default": true
      }
    }
  },
  "server_motd": {
    "description": "服务器MOTD",
    "type": "object",
    "hint": "服务器MOTD展示设置",
    "items": {
      "is_opened": {
        "description": "服务器MOTD显示",
        "hint": "是否开启服务器MOTD在信息卡片中渲染",
        "type": "bool",
        "default": true
      }
    }
  },
  "player_count": {
    "description": "服务器在线人数",
    "type": "object",
    "hint": "服务器在线人数展示设置",
    "items": {
      "is_opened": {
        "description": "服务器在线人数显示",
        "hint": "是否开启服务器在线人数在信息卡片中渲染",
        "type": "bool",
        "default": true
      }
    }
  }
}